/**
 * Hook rỗng (no-op) sau khi đã gỡ bỏ chức năng auto-scroll/page-snapping
 * và quay lại chế độ cuộn dọc tự nhiên của trình duyệt.
 */
export function useDesktopSectionPager() {
  // Không làm gì cả
}
